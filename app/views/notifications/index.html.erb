<%= turbo_frame_tag "notifications" do %>
  <h1 class="inline-block mb-2 font-extrabold text-3xl tracking-tight text-slate-800 dark:text-white">Notifications</h1>
  <ul>
    <%= render @notifications %>
  </ul>
<% end %>
