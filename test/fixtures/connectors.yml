# == Schema Information
#
# Table name: connectors
#
#  id           :uuid             not null, primary key
#  author       :string
#  enabled      :boolean          default(FALSE), not null
#  github       :string
#  name         :string           not null
#  redirect_url :string           not null
#  settings     :jsonb            not null
#  tags         :string           default([]), is an Array
#  verified     :boolean          default(FALSE), not null
#  version      :string
#  website      :string
#  created_at   :datetime         not null
#  updated_at   :datetime         not null
#
# Indexes
#
#  index_connectors_on_enabled   (enabled)
#  index_connectors_on_name      (name) UNIQUE
#  index_connectors_on_tags      (tags) USING gin
#  index_connectors_on_verified  (verified)
#

one:
  name: Sample Connector One
  description: Description for Sample Connector One
  github: https://github.com/sample/connector-one
  version: '1.0.0'
  website: https://connector-one.example.com
  author: Author One

two:
  name: Sample Connector Two
  description: Description for Sample Connector Two
  github: https://github.com/sample/connector-two
  version: '1.1.0'
  website: https://connector-two.example.com
  author: Author Two
